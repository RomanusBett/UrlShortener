import styles from './home.module.css';

const Button = ()=>{
    return (
        
         <button className={styles.shorten_button}>
            SHORTEN URL
         </button>
    )
}
export default Button;